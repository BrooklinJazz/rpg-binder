import React from "react";
import styled from "styled-components";
import { surface1 } from "../../../common/styles";

export const Entry = styled.section`
  background-color: ${surface1};
  grid-area: entry;
`;
