import React from "react";
import { GridTemplateAreas } from "../../../../common/constants";

const ReferenceSidebar = () => {
  return <div style={{border: "solid 1px blue"}} className={GridTemplateAreas.ENTRY_SIDEBAR}>Ref Sidebar</div>;
};

export default ReferenceSidebar;
