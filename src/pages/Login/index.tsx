import React from "react";

import { LoginForm } from "./Form";
import { Grid } from "./Grid";

const LoginPage = () => {
  return (
    <Grid>
      <LoginForm />
    </Grid>
  );
};

export default LoginPage;
